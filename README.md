# haproxy-test-reused-connections-with-set-src
Testing reusing tcp connections for several HTTP requests. We will set the src ip in the first request and see if the src in the second request is modified.
